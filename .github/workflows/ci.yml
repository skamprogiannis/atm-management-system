name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Build with warnings as errors
        run: make

      - name: Confirm generated artifacts can be cleaned
        run: make fclean
